# Исправление проблем с профилем

## Проблемы

1. **Method not Allowed при обновлении профиля** - при изменении города и обновлении анкеты
2. **Моковые данные вместо реальных** - на вкладке анкеты показываются моки вместо данных из БД

## ✅ Что исправлено

### 1. Улучшено логирование ошибок

- Добавлена специальная обработка для 405 ошибки
- Логируется URL, метод, статус при ошибке
- Видно в консоли что именно не работает

### 2. Убраны моковые данные как fallback

**Было:** При любой ошибке показывались моковые данные  
**Стало:** При ошибке показывается пустой список (моки только если нет userInfo.id)

Это поможет увидеть реальные проблемы вместо скрытия их моками.

## 🔍 Диагностика

### Проверь Console в браузере

1. Открой DevTools (F12) → Console
2. Попробуй обновить профиль
3. Ищи логи:
   - `=== SENDING PROFILE REQUEST ===`
   - `Full URL: ...`
   - `Method: POST`
   - Если ошибка 405: `=== METHOD NOT ALLOWED ERROR ===`

### Проверь Network tab

1. DevTools (F12) → Network
2. Попробуй обновить профиль
3. Найди запрос к `/api/profiles`
4. Проверь:
   - **Request URL**: должен быть правильный
   - **Request Method**: `POST`
   - **Status Code**: что показывает?
   - **Response**: что в ответе?

### Проверь загрузку профиля

В Console должны быть логи:
- `Loading profile from: ...`
- `Profile load response status: ...`

Если статус не 200, значит проблема с загрузкой профиля.

## 🚨 Возможные причины Method not Allowed

1. **Роут не зарегистрирован** - проверь что роуты закоммичены и задеплоены
2. **CORS блокирует** - проверь CORS_ORIGINS в Koyeb
3. **Неправильный URL** - проверь что REACT_APP_API_BASE_URL правильный
4. **Catch-all роут перехватывает** - но это должно быть исправлено

## 📝 Что сделать

1. **Закоммить изменения:**
   ```bash
   git add src/pages/ProfileForm.jsx src/pages/Profiles.jsx
   git commit -m "Fix: улучшено логирование, убраны моки как fallback"
   git push
   ```

2. **Пересобрать фронтенд в Vercel**

3. **Проверить:**
   - Открой Console и Network tab
   - Попробуй обновить профиль
   - Посмотри что показывает в логах

4. **Если все еще Method not Allowed:**
   - Проверь логи в Koyeb
   - Проверь что роуты задеплоены
   - Проверь через Swagger UI (`/docs`)

## 🎯 Ожидаемый результат

- ✅ При обновлении профиля НЕТ ошибки 405
- ✅ Показываются реальные данные из БД, не моки
- ✅ В Console видны подробные логи
- ✅ При ошибках показывается пустой список, не моки

