# Как найти URL вашего бэкенда

URL бэкенда зависит от того, где вы его задеплоили. Вот как найти его на разных платформах:

## 🚂 Railway

1. Зайдите на [railway.app](https://railway.app)
2. Откройте ваш проект
3. Выберите сервис с бэкендом
4. Перейдите в раздел **Settings** → **Networking**
5. Найдите **Public Domain** или **Custom Domain**
6. URL будет выглядеть так: `https://your-app-name.up.railway.app`

**Или:**
- В разделе **Deployments** → выберите последний деплой
- В логах будет URL типа: `https://your-app-name.up.railway.app`

## 🎨 Render

1. Зайдите на [render.com](https://render.com)
2. Откройте ваш проект (Web Service)
3. В верхней части страницы будет **URL**
4. URL будет выглядеть так: `https://your-app-name.onrender.com`

**Или:**
- В разделе **Settings** → **Environment** → найдите переменную `PUBLIC_URL`
- Или просто скопируйте URL из шапки страницы сервиса

## 🟣 Heroku

1. Зайдите на [heroku.com](https://heroku.com)
2. Откройте ваше приложение
3. В правом верхнем углу будет кнопка **Open app**
4. URL будет выглядеть так: `https://your-app-name.herokuapp.com`

**Или:**
- В разделе **Settings** → **Domains** → найдите URL

## 🌐 Другие платформы

### PythonAnywhere
- URL: `https://your-username.pythonanywhere.com`

### Fly.io
- URL: `https://your-app-name.fly.dev`

### DigitalOcean App Platform
- URL: `https://your-app-name.ondigitalocean.app`

### VPS (свой сервер)
- URL: `https://your-domain.com` или `https://your-ip-address`

## ✅ Проверка, что URL правильный

После того как нашли URL, проверьте его:

```bash
# Откройте в браузере или через curl:
https://your-backend-url.com/health

# Должен вернуть:
{"status":"ok"}
```

Или:

```bash
# Проверка корневого эндпоинта:
https://your-backend-url.com/

# Должен вернуть:
{"message":"Networking App API"}
```

## 🔧 Если бэкенд еще не задеплоен

Если вы еще не задеплоили бэкенд, вот быстрые варианты:

### Вариант 1: Railway (рекомендуется, бесплатно)
1. Зайдите на [railway.app](https://railway.app)
2. **New Project** → **Deploy from GitHub**
3. Выберите репозиторий
4. Выберите папку `backend_python`
5. Railway автоматически определит Python и задеплоит
6. URL будет в разделе **Settings** → **Networking**

### Вариант 2: Render (бесплатно)
1. Зайдите на [render.com](https://render.com)
2. **New** → **Web Service**
3. Подключите GitHub репозиторий
4. Укажите:
   - **Root Directory**: `backend_python`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
5. URL будет в шапке страницы сервиса

### Вариант 3: Для тестирования - ngrok (временно)
Если нужно быстро протестировать локально:

```bash
# Запустите бэкенд локально
cd backend_python
uvicorn app.main:app --reload --port 8080

# В другом терминале запустите ngrok
ngrok http 8080

# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
# Это и будет ваш временный URL бэкенда
```

## 📝 Примеры правильных URL

```
✅ Правильно:
https://networking-app.up.railway.app
https://networking-backend.onrender.com
https://my-app.herokuapp.com

❌ Неправильно:
http://localhost:8080  (работает только локально)
http://your-url.com    (нужен HTTPS)
```

## 🎯 После того как нашли URL

1. Скопируйте URL бэкенда
2. На Vercel добавьте переменную окружения:
   ```
   REACT_APP_API_BASE_URL=https://ваш-backend-url.com
   ```
3. Пересоберите проект на Vercel
4. Готово!

