# Что делать дальше после создания БД

## ✅ Шаг 1: Настроить бэкенд

### 1.1. Откройте файл `backend/src/main/resources/application.properties`

### 1.2. Измените настройки базы данных:

```properties
# Замените "postgres" на ваш пароль от PostgreSQL
spring.datasource.password=ваш_пароль_от_postgres
```

Если вы использовали другой пользователь (не postgres), измените также:
```properties
spring.datasource.username=ваш_пользователь
```

### 1.3. Проверьте остальные настройки (обычно можно оставить как есть)

---

## ✅ Шаг 2: Запустить бэкенд

### Вариант A: Через командную строку

1. Откройте терминал в папке `backend`:
```bash
cd backend
```

2. Запустите Maven:
```bash
# Windows
mvnw.cmd spring-boot:run

# macOS/Linux
./mvnw spring-boot:run
```

Если Maven не установлен, скачайте Maven Wrapper или установите Maven.

### Вариант B: Через IDE (IntelliJ IDEA / Eclipse / VS Code)

1. Откройте папку `backend` как проект
2. Дождитесь загрузки зависимостей
3. Найдите файл `NetworkingApplication.java`
4. Правой кнопкой → **Run** или нажмите Shift+F10

### Проверка работы бэкенда:

Откройте браузер: http://localhost:8080/api/profiles?userId=123

Должен вернуться пустой массив `{"content":[],"totalElements":0,...}` - это нормально, значит бэкенд работает!

---

## ✅ Шаг 3: Настроить фронтенд

### 3.1. Создайте файл `.env` в корне проекта (рядом с package.json)

Создайте файл `.env` со следующим содержимым:

```env
REACT_APP_API_BASE_URL=http://localhost:8080
```

### 3.2. Установите зависимости (если еще не установлены)

```bash
npm install
```

---

## ✅ Шаг 4: Запустить фронтенд

```bash
npm start
```

Приложение откроется на http://localhost:3000

---

## ✅ Шаг 5: Протестировать

### 5.1. Создать профиль

1. Откройте http://localhost:3000
2. Перейдите в "Добавить мой профиль"
3. Заполните форму
4. Нажмите "Сохранить"

### 5.2. Проверить в базе данных

В pgAdmin или psql выполните:
```sql
SELECT * FROM profiles;
```

Должен появиться ваш профиль!

### 5.3. Проверить API напрямую

Откройте в браузере:
```
http://localhost:8080/api/profiles?userId=ваш_userId
```

Должны вернуться профили (если есть другие пользователи).

---

## 🔧 Решение проблем

### Бэкенд не запускается

**Ошибка: "Cannot connect to database"**
- Проверьте пароль в `application.properties`
- Убедитесь, что PostgreSQL запущен
- Проверьте, что база `networking_app` существует

**Ошибка: "Port 8080 already in use"**
- Измените порт в `application.properties`: `server.port=8081`
- Или остановите другое приложение на порту 8080

### Фронтенд не подключается к бэкенду

**Ошибка CORS:**
- Убедитесь, что бэкенд запущен
- Проверьте `REACT_APP_API_BASE_URL` в `.env`
- Перезапустите фронтенд после изменения `.env`

**Ошибка 404:**
- Проверьте URL в `src/config/api.js`
- Убедитесь, что бэкенд работает на правильном порту

### База данных пустая

Это нормально! Профили создаются через форму в приложении.

---

## 📝 Чек-лист готовности

- [ ] База данных создана и таблицы созданы
- [ ] `application.properties` настроен (пароль от БД)
- [ ] Бэкенд запущен на http://localhost:8080
- [ ] Файл `.env` создан с `REACT_APP_API_BASE_URL=http://localhost:8080`
- [ ] Фронтенд запущен на http://localhost:3000
- [ ] Можно создать профиль через форму
- [ ] Профиль сохраняется в БД

---

## 🚀 Следующие шаги

После того как всё работает локально:

1. **Создать Telegram бота** (см. `TELEGRAM_SETUP.md`)
2. **Задеплоить фронтенд** на Vercel/Netlify
3. **Задеплоить бэкенд** на Heroku/VPS
4. **Обновить URL** в настройках Telegram бота

---

## 💡 Полезные команды

### Проверить что бэкенд работает:
```bash
curl http://localhost:8080/api/profiles?userId=123
```

### Посмотреть логи бэкенда:
Логи выводятся в консоль где запущен бэкенд

### Посмотреть данные в БД:
```sql
-- Все профили
SELECT * FROM profiles;

-- Все свайпы
SELECT * FROM swipes;

-- Все мэтчи
SELECT * FROM matches;
```

---

Готово! Теперь у вас работает полный стек: БД → Бэкенд → Фронтенд 🎉

