# Настройка для Cloudflare Tunnel

## ✅ Фронтенд настроен

Файл `.env` создан с URL вашего бэкенда:
```
REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com
```

## 🔧 Что нужно сделать на сервере (бэкенд)

### 1. Обновите CORS настройки бэкенда

В файле `.env` бэкенда (`backend_python/.env`) добавьте:

```env
CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://rica-student-trusted-puzzle.trycloudflare.com
PRODUCTION=true
```

### 2. Убедитесь, что бэкенд запущен

```bash
cd backend_python
uvicorn app.main:app --host 0.0.0.0 --port 8080
```

**Важно:** Используйте `--host 0.0.0.0`, а не `127.0.0.1`, чтобы бэкенд был доступен через туннель.

### 3. Проверьте туннель

Убедитесь, что Cloudflare Tunnel активен и проксирует на `http://localhost:8080`:

```bash
# Проверьте статус туннеля
cloudflared tunnel list

# Или проверьте логи
cloudflared tunnel --url http://localhost:8080
```

### 4. Проверьте доступность

Откройте в браузере:
```
https://rica-student-trusted-puzzle.trycloudflare.com/health
```

Должен вернуться: `{"status":"ok"}`

## 📚 Подробные инструкции

- **CLOUDFLARE_TUNNEL_SETUP.md** - общая информация о настройке
- **CLOUDFLARE_TUNNEL_BACKEND_SETUP.md** - детальная настройка бэкенда

## ⚠️ Важно

1. **Перезапустите бэкенд** после изменения `.env`
2. **Туннель должен быть активен** пока работает бэкенд
3. **CORS должен разрешать** домен туннеля

## 🔄 После настройки

1. Перезапустите фронтенд: `npm start`
2. Попробуйте создать профиль
3. Если ошибка - проверьте компонент диагностики (показывается автоматически)

