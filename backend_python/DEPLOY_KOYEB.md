# Инструкция по деплою бэкенда на Koyeb

## Шаг 1: Подготовка проекта

1. Убедитесь, что все изменения закоммичены в Git:
```bash
git add .
git commit -m "Prepare for Koyeb deployment"
git push
```

2. Проверьте, что файлы на месте:
   - ✅ `requirements.txt` - зависимости Python
   - ✅ `Procfile` - команда запуска
   - ✅ `.python-version` - версия Python (создан автоматически, содержит `3.11`)
   - ✅ `backend_python/app/main.py` - главный файл приложения

3. Файл `.python-version` уже создан в `backend_python/` и указывает Python 3.11

## Шаг 2: Создание аккаунта на Koyeb

1. Перейдите на https://www.koyeb.com
2. Зарегистрируйтесь или войдите (можно через GitHub)
3. Нажмите "Create App" или "New App"

## Шаг 3: Подключение репозитория

1. Выберите "GitHub" (или другой Git-провайдер)
2. Выберите ваш репозиторий
3. Выберите ветку (обычно `main` или `master`)
4. Нажмите "Connect"

## Шаг 4: Настройка приложения

### Основные настройки:

- **Name**: `networking-app-backend` (или любое другое имя)
- **Region**: Выберите ближайший регион (например, `fra` для Frankfurt)
- **Branch**: `main` (или ваша основная ветка)

### Build Settings:

⚠️ **ВАЖНО**: Koyeb автоматически перемещает содержимое `backend_python` в корень рабочей директории, поэтому команды должны быть без `cd backend_python`:

- **Build Command**: `pip install -r requirements.txt`
- **Run Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2`

### Root Directory:

- Установите `backend_python` как корневую директорию (Root Directory)
- Koyeb автоматически переместит содержимое этой директории в корень при билде

## Шаг 5: Настройка переменных окружения

В разделе "Environment Variables" или "Secrets" добавьте следующие переменные:

### ⚠️ ОБЯЗАТЕЛЬНЫЕ переменные:

#### 1. DATABASE_URL
```
DATABASE_URL=postgresql://user:password@host:port/database
```
- Если используете Neon PostgreSQL:
  - Зайдите в Neon Dashboard
  - Скопируйте Connection String
  - Формат: `postgresql://user:pass@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require`
- Если используете другую БД, скопируйте connection string оттуда

#### 2. JWT_SECRET
```
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-random-string
```
- ⚠️ **КРИТИЧЕСКИ ВАЖНО**: Используйте уникальный случайный ключ минимум 32 символа
- Сгенерировать можно командой:
  ```bash
  python -c "import secrets; print(secrets.token_urlsafe(32))"
  ```
- Или онлайн генератором: https://randomkeygen.com/

### 📦 Cloudinary (для загрузки изображений):

#### Вариант 1: Отдельные переменные (рекомендуется)
```
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
```

#### Вариант 2: Одна переменная (альтернатива)
```
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name
```

**Как получить:**
1. Зайдите на https://cloudinary.com
2. Зарегистрируйтесь или войдите
3. В Dashboard найдите:
   - **Cloud name** → `CLOUDINARY_CLOUD_NAME`
   - **API Key** → `CLOUDINARY_API_KEY`
   - **API Secret** → `CLOUDINARY_API_SECRET`

### 🔧 Опциональные переменные:

#### 3. CORS_ORIGINS
```
CORS_ORIGINS=https://web.telegram.org,https://telegram.org
```
- Разрешенные домены для CORS через запятую
- Для Telegram Mini Apps обычно нужны домены Telegram

#### 4. FRONTEND_URL
```
FRONTEND_URL=https://your-frontend-url.com
```
- URL вашего фронтенда, если он развернут отдельно
- Например: `https://your-app.vercel.app`

#### 5. MAX_FILE_SIZE
```
MAX_FILE_SIZE=5242880
```
- Максимальный размер загружаемого файла в байтах
- По умолчанию: `5242880` (5MB)
- Можно увеличить до `10485760` (10MB) или больше

#### 6. TELEGRAM_BOT_TOKEN
```
TELEGRAM_BOT_TOKEN=your_bot_token
```
- Токен Telegram бота (если используете)
- Получить можно у @BotFather в Telegram

## Шаг 6: Полный список переменных для копирования

Скопируйте и заполните своими значениями:

```
DATABASE_URL=postgresql://user:password@host:port/database
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-random-string
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
CORS_ORIGINS=https://web.telegram.org,https://telegram.org
FRONTEND_URL=https://your-frontend-url.com
MAX_FILE_SIZE=5242880
TELEGRAM_BOT_TOKEN=your_bot_token
```

## Шаг 7: Деплой

1. Нажмите "Deploy" или "Create App"
2. Дождитесь завершения билда (обычно 2-5 минут)
3. Проверьте логи на наличие ошибок

## Шаг 8: Проверка работы

После успешного деплоя проверьте:

1. **Health Check:**
   ```
   https://your-app-name.koyeb.app/health
   ```
   Должен вернуть: `{"status": "ok"}`

2. **API Documentation:**
   ```
   https://your-app-name.koyeb.app/docs
   ```
   Должна открыться Swagger UI с документацией API

3. **Alternative API Docs:**
   ```
   https://your-app-name.koyeb.app/redoc
   ```
   Альтернативная документация в формате ReDoc

## Шаг 9: Обновление фронтенда

После деплоя обновите URL API во фронтенде:

1. Найдите файл конфигурации API (например, `src/config/api.js` или `src/utils/api.js`)
2. Замените URL на ваш Koyeb URL:
   ```javascript
   const API_URL = 'https://your-app-name.koyeb.app';
   ```
3. Пересоберите и задеплойте фронтенд

## Шаг 10: Настройка кастомного домена (опционально)

1. В Koyeb Dashboard перейдите в настройки приложения
2. Найдите раздел "Domains"
3. Добавьте ваш домен
4. Настройте DNS записи согласно инструкциям Koyeb

## 🔍 Решение проблем

### Ошибка: "DATABASE_URL environment variable is required"
- Убедитесь, что переменная `DATABASE_URL` добавлена в Environment Variables
- Проверьте, что значение скопировано полностью (включая `?sslmode=require` для Neon)

### Ошибка: "Cloudinary не настроен"
- Проверьте, что все три переменные Cloudinary добавлены:
  - `CLOUDINARY_CLOUD_NAME`
  - `CLOUDINARY_API_KEY`
  - `CLOUDINARY_API_SECRET`
- Убедитесь, что значения скопированы правильно (без пробелов)

### Ошибка: "Invalid cloud name"
- Проверьте, что `CLOUDINARY_CLOUD_NAME` точно совпадает с Cloud name в Cloudinary Dashboard
- Cloud name должен быть без пробелов и специальных символов

### Ошибка: "cd: backend_python: No such file or directory"
- ⚠️ **Koyeb автоматически перемещает содержимое `backend_python` в корень**
- Убедитесь, что Build Command: `pip install -r requirements.txt` (БЕЗ `cd backend_python`)
- Убедитесь, что Run Command: `uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2` (БЕЗ `cd backend_python`)
- Проверьте, что Root Directory установлен в `backend_python`

### Приложение не запускается
- Проверьте логи в Koyeb Dashboard
- Убедитесь, что команда запуска правильная:
  ```
  uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2
  ```
- Проверьте, что `Procfile` существует и содержит правильную команду

### CORS ошибки
- Добавьте домены в `CORS_ORIGINS`
- Убедитесь, что домены указаны через запятую без пробелов:
  ```
  CORS_ORIGINS=https://web.telegram.org,https://telegram.org
  ```

## 📝 Чеклист перед деплоем

- [ ] Все изменения закоммичены и запушены в Git
- [ ] `requirements.txt` содержит все зависимости
- [ ] `Procfile` настроен правильно
- [ ] `DATABASE_URL` готов (из Neon или другого источника)
- [ ] `JWT_SECRET` сгенерирован (минимум 32 символа, уникальный!)
- [ ] `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET` готовы
- [ ] Все переменные окружения добавлены в Koyeb
- [ ] Health check работает после деплоя
- [ ] API docs доступны
- [ ] Фронтенд обновлен с новым API URL

## 🔗 Полезные ссылки

- Koyeb Dashboard: https://www.koyeb.com/apps
- Koyeb Documentation: https://www.koyeb.com/docs
- Neon PostgreSQL: https://neon.tech
- Cloudinary Dashboard: https://cloudinary.com/console

