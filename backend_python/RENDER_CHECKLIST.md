# Чеклист для деплоя на Render

## ✅ Перед деплоем

- [ ] Все изменения закоммичены и запушены в Git
- [ ] `requirements.txt` содержит все зависимости
- [ ] `Procfile` настроен правильно
- [ ] `DATABASE_URL` готов (из Neon или другого источника)
- [ ] `CLOUDINARY_URL` готов (если используете Cloudinary)

## ✅ Настройки Render Web Service

- [ ] Репозиторий подключен
- [ ] **Root Directory**: `backend_python` ⚠️
- [ ] **Build Command**: `pip install -r requirements.txt`
- [ ] **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2`
- [ ] **Environment**: `Python 3`

## ✅ Environment Variables

**Обязательные:**
- [ ] `DATABASE_URL` добавлен
- [ ] `JWT_SECRET` добавлен (минимум 32 символа, уникальный!)

**Cloudinary (если используется):**
- [ ] `CLOUDINARY_CLOUD_NAME` добавлен
- [ ] `CLOUDINARY_API_KEY` добавлен
- [ ] `CLOUDINARY_API_SECRET` добавлен
- [ ] ИЛИ `CLOUDINARY_URL` добавлен (альтернатива трем переменным выше)

**Опциональные:**
- [ ] `CORS_ORIGINS` добавлен
- [ ] `FRONTEND_URL` добавлен (если фронтенд развернут отдельно)
- [ ] `MAX_FILE_SIZE` добавлен (по умолчанию 5MB)
- [ ] `TELEGRAM_BOT_TOKEN` добавлен (если используется)

## ✅ После деплоя

- [ ] Health check работает: `https://your-app.onrender.com/health`
- [ ] API docs доступны: `https://your-app.onrender.com/docs`
- [ ] Логи не показывают ошибок
- [ ] Фронтенд обновлен с новым API URL

## 🔍 Проверка работы

1. Откройте: `https://your-app.onrender.com/health`
   - Ожидается: `{"status":"ok"}`

2. Откройте: `https://your-app.onrender.com/docs`
   - Должна открыться Swagger документация

3. Проверьте логи в Render Dashboard
   - Не должно быть ошибок подключения к БД
   - Не должно быть ошибок импорта модулей

## 🐛 Частые проблемы

### Ошибка "DATABASE_URL environment variable is required"
- ✅ Проверьте, что переменная добавлена в Environment Variables
- ✅ Проверьте формат (должен начинаться с `postgresql://`)

### Ошибка подключения к БД
- ✅ Убедитесь, что БД запущена
- ✅ Проверьте, что используете правильный URL (Internal или External)
- ✅ Проверьте, что в URL правильный порт

### CORS ошибки
- ✅ Добавьте ваш фронтенд URL в `CORS_ORIGINS`
- ✅ Или добавьте `FRONTEND_URL` переменную

### Module not found
- ✅ Проверьте, что все зависимости в `requirements.txt`
- ✅ Убедитесь, что `Root Directory` = `backend_python`

### Приложение не запускается
- ✅ Проверьте логи в Render Dashboard
- ✅ Убедитесь, что `Start Command` правильный
- ✅ Проверьте, что используется `$PORT` в команде запуска

