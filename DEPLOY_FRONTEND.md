# Развертывание фронтенда

## 🎯 Варианты развертывания

### Вариант 1: Статический хостинг (Рекомендуется)

Фронтенд - это статическое React приложение, его можно развернуть на:
- **Vercel** (рекомендуется)
- **Netlify**
- **GitHub Pages**
- **Любой статический хостинг**

### Вариант 2: На том же сервере (через nginx)

Можно развернуть фронтенд на том же сервере, где бэкенд, через nginx.

## 📦 Подготовка к развертыванию

### 1. Соберите фронтенд

На вашем **локальном компьютере** (где находится исходный код):

```bash
# Убедитесь, что .env файл настроен
cat .env
# Должно быть: REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com

# Соберите проект
npm run build
```

Это создаст папку `build/` с готовыми статическими файлами.

### 2. Развертывание на Vercel (Самый простой способ)

1. Установите Vercel CLI:
   ```bash
   npm install -g vercel
   ```

2. Войдите в Vercel:
   ```bash
   vercel login
   ```

3. Разверните проект:
   ```bash
   vercel
   ```

4. Установите переменную окружения в Vercel Dashboard:
   - Перейдите в Settings → Environment Variables
   - Добавьте: `REACT_APP_API_BASE_URL` = `https://rica-student-trusted-puzzle.trycloudflare.com`

### 3. Развертывание на том же сервере (через nginx)

Если хотите развернуть на том же сервере:

#### Шаг 1: Скопируйте build на сервер

```bash
# На локальном компьютере
npm run build
scp -r build/* root@your-server:/var/www/frontend/
```

#### Шаг 2: Настройте nginx

Создайте конфигурацию nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/frontend;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API проксирование (опционально, если хотите проксировать через nginx)
    location /api/ {
        proxy_pass https://rica-student-trusted-puzzle.trycloudflare.com;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### Шаг 3: Настройте переменные окружения

Перед сборкой убедитесь, что `.env` файл содержит правильный URL:

```env
REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com
```

**Важно:** Переменные окружения встраиваются в `build` во время сборки, поэтому нужно пересобрать проект после изменения `.env`.

## 🔧 Если фронтенд уже на сервере

Если вы хотите запустить фронтенд на сервере (не рекомендуется для продакшена):

### 1. Найдите директорию с фронтендом

```bash
# Найдите директорию с package.json
find /root -name "package.json" -type f 2>/dev/null

# Или проверьте стандартные места
ls -la /var/www/
ls -la /home/
ls -la /opt/
```

### 2. Перейдите в правильную директорию

```bash
# Если фронтенд в /var/www/frontend
cd /var/www/frontend

# Или если в /home/user/frontend
cd /home/user/frontend

# Проверьте, что package.json есть
ls package.json
```

### 3. Установите зависимости и запустите

```bash
npm install
npm start
```

**Но помните:** `npm start` запускает dev-сервер, который не предназначен для продакшена!

## ✅ Рекомендуемый подход

### Для продакшена:

1. **Соберите фронтенд локально:**
   ```bash
   npm run build
   ```

2. **Разверните на Vercel/Netlify:**
   - Самый простой способ
   - Автоматический HTTPS
   - CDN из коробки
   - Бесплатный план достаточен для начала

3. **Или разверните на сервере через nginx:**
   - Больше контроля
   - Нужно настроить nginx и SSL

### Для разработки:

Используйте `npm start` на **локальном компьютере**, а не на сервере.

## 🚀 Быстрый старт с Vercel

```bash
# 1. Установите Vercel CLI
npm install -g vercel

# 2. Войдите
vercel login

# 3. Разверните (из корня проекта фронтенда)
vercel

# 4. Установите переменную окружения в Vercel Dashboard
# REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com
```

## 📝 Важно помнить

- **Фронтенд и бэкенд могут быть на разных серверах**
- **Переменные окружения встраиваются в build** - нужно пересобрать после изменения
- **Для продакшена используйте `npm run build`**, а не `npm start`
- **`npm start` только для разработки** на локальной машине

