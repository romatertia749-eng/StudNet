# Быстрое исправление проблемы подключения

## 🔍 Проблема:

Health check не проходит через Cloudflare Tunnel, поэтому форма не отправляется.

## ✅ Решения (выполните по порядку):

### 1. Проверьте доступность туннеля:

**На вашем компьютере** откройте в браузере:
```
https://rica-student-trusted-puzzle.trycloudflare.com/health
```

Если не открывается - туннель недоступен извне.

### 2. Проверьте и обновите CORS на бэкенде:

```bash
ssh root@155.212.170.255
cat /var/www/studnet/backend_python/.env | grep CORS_ORIGINS
```

Если домена туннеля нет, добавьте:

```bash
nano /var/www/studnet/backend_python/.env
```

Добавьте/обновите:
```env
CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://rica-student-trusted-puzzle.trycloudflare.com
PRODUCTION=true
```

Сохраните (Ctrl+O, Enter, Ctrl+X) и перезапустите:

```bash
systemctl restart studnet-backend.service
systemctl status studnet-backend.service
```

### 3. Проверьте логи бэкенда:

```bash
# Смотрите логи в реальном времени
tail -f /var/log/studnet/backend_error.log
```

Попробуйте создать профиль в приложении - посмотрите, что появится в логах.

### 4. Проверьте Cloudflare Tunnel:

```bash
# На сервере
ps aux | grep cloudflared
curl https://rica-student-trusted-puzzle.trycloudflare.com/health
```

## 🎯 Наиболее вероятная причина:

**CORS не настроен** для домена туннеля на бэкенде. Выполните шаг 2 выше.

## 📋 Если не помогает:

Отключите health check временно (для теста):

В `src/pages/ProfileForm.jsx` закомментируйте строки 323-334 (health check блок).

Но лучше исправить CORS - это правильное решение.

