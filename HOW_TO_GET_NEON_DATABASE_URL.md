# Как получить DATABASE_URL из Neon

## Шаг 1: Войдите в Neon Dashboard

1. Перейдите на https://console.neon.tech
2. Войдите в свой аккаунт

## Шаг 2: Выберите проект

1. В списке проектов выберите нужный проект (или создайте новый)
2. Нажмите на название проекта

## Шаг 3: Найдите Connection String

### Вариант 1: На главной странице проекта

1. На главной странице проекта вы увидите карточку с информацией о базе данных
2. Найдите раздел **"Connection Details"** или **"Connection string"**
3. Вы увидите несколько вариантов:
   - **Pooled connection** (рекомендуется для production)
   - **Direct connection** (для простых случаев)

### Вариант 2: Через вкладку "Connection Details"

1. В левом меню найдите вкладку **"Connection Details"** или **"Connection"**
2. Там будут показаны все варианты подключения

## Шаг 4: Выберите нужный формат

### Для Render рекомендуется использовать:

**Pooled connection** (если доступен):
```
postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
```

**Direct connection**:
```
postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
```

### Важные моменты:

1. **SSL Mode**: Убедитесь, что в URL есть `?sslmode=require` или `?sslmode=prefer`
2. **Пароль**: Если пароль не указан, нажмите "Show" или "Reveal" чтобы увидеть его
3. **Копирование**: Нажмите кнопку "Copy" рядом с Connection String

## Шаг 5: Скопируйте Connection String

1. Нажмите на кнопку **"Copy"** рядом с Connection String
2. Или выделите весь текст и скопируйте (Ctrl+C)

## Шаг 6: Добавьте в Render

1. Откройте ваш Web Service в Render Dashboard
2. Перейдите в раздел **"Environment"**
3. Найдите или создайте переменную **`DATABASE_URL`**
4. Вставьте скопированный Connection String
5. Сохраните изменения

## Пример Connection String

```
postgresql://neondb_owner:npg_xxxxxxxxxxxx@ep-icy-xxx-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
```

Где:
- `neondb_owner` - имя пользователя
- `npg_xxxxxxxxxxxx` - пароль
- `ep-icy-xxx-xxx.us-east-2.aws.neon.tech` - хост
- `neondb` - имя базы данных
- `?sslmode=require` - параметры SSL

## 🔒 Безопасность

⚠️ **Важно**: 
- Никогда не коммитьте Connection String в Git
- Используйте только Environment Variables в Render
- Если пароль скомпрометирован, сгенерируйте новый в Neon

## 🔄 Если нужно изменить пароль

1. В Neon Dashboard → ваш проект
2. Перейдите в **"Settings"** → **"Database"**
3. Найдите опцию **"Reset password"** или **"Change password"**
4. Сгенерируйте новый пароль
5. Обновите `DATABASE_URL` в Render с новым паролем

## 📝 Проверка подключения

После добавления `DATABASE_URL` в Render:

1. Проверьте логи деплоя - не должно быть ошибок подключения к БД
2. Откройте `https://your-app.onrender.com/health` - должен вернуться `{"status":"ok"}`
3. Если есть ошибки, проверьте:
   - Правильность скопированного URL
   - Наличие `?sslmode=require` в конце
   - Что база данных активна в Neon

## 🆘 Проблемы

### "Connection refused"
- Проверьте, что база данных не приостановлена в Neon
- Убедитесь, что используете правильный хост

### "Authentication failed"
- Проверьте правильность пароля
- Попробуйте сгенерировать новый пароль

### "Database does not exist"
- Проверьте имя базы данных в Connection String
- Убедитесь, что база данных создана в Neon


