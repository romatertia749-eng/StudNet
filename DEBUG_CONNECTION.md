# Отладка проблемы подключения

## 🔍 Что видно из ошибки:

Приложение пытается отправить запрос, но получает ошибку подключения. Кнопка показывает "Сохранение..." - значит запрос отправляется.

## ✅ Проверки:

### 1. Проверьте доступность туннеля из браузера:

Откройте в браузере (на вашем компьютере):
```
https://rica-student-trusted-puzzle.trycloudflare.com/health
```

Должно вернуться: `{"status":"ok","database":"connected","time":"0.02s"}`

Если не работает - туннель недоступен извне.

### 2. Проверьте CORS на бэкенде:

```bash
ssh root@155.212.170.255
cat /var/www/studnet/backend_python/.env | grep CORS_ORIGINS
```

Должно быть:
```
CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://rica-student-trusted-puzzle.trycloudflare.com
```

Если нет - добавьте и перезапустите.

### 3. Проверьте логи бэкенда при попытке создания:

```bash
# На сервере, в реальном времени
tail -f /var/log/studnet/backend_access.log
tail -f /var/log/studnet/backend_error.log
```

Затем попробуйте создать профиль в приложении и посмотрите, что появится в логах.

### 4. Проверьте консоль браузера:

В приложении откройте DevTools (если возможно) или проверьте:
- Какой URL используется для запроса
- Какая ошибка CORS или сети

## 🔧 Быстрое исправление:

### Шаг 1: Проверьте туннель

```bash
# На сервере
curl https://rica-student-trusted-puzzle.trycloudflare.com/health
```

### Шаг 2: Обновите CORS

```bash
ssh root@155.212.170.255
nano /var/www/studnet/backend_python/.env
```

Добавьте/обновите:
```env
CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://rica-student-trusted-puzzle.trycloudflare.com
PRODUCTION=true
```

Перезапустите:
```bash
systemctl restart studnet-backend.service
```

### Шаг 3: Проверьте логи

```bash
# Смотрите логи в реальном времени
tail -f /var/log/studnet/backend_error.log
```

Попробуйте создать профиль и посмотрите, что появится в логах.

