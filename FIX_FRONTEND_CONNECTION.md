# Исправление проблемы подключения фронтенда

## 🔍 Проблема:

При создании анкеты выходит ошибка "не удалось подключиться к серверу", хотя бэкенд работает.

## ✅ Решения:

### 1. Проверьте доступность через Cloudflare Tunnel:

```bash
# На сервере
curl https://rica-student-trusted-puzzle.trycloudflare.com/health

# Должен вернуться: {"status":"ok","database":"connected","time":"0.02s"}
```

### 2. Перезапустите фронтенд после изменения .env:

**ВАЖНО:** После изменения `.env` нужно перезапустить dev-сервер!

```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова
npm start
```

### 3. Проверьте консоль браузера:

Откройте DevTools (F12) → Console и проверьте:
- Какой URL используется для запросов
- Какие ошибки CORS или сети

### 4. Проверьте CORS на бэкенде:

```bash
# На сервере проверьте .env бэкенда
cat /var/www/studnet/backend_python/.env | grep CORS

# Должно быть:
# CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://rica-student-trusted-puzzle.trycloudflare.com
```

Если нет - добавьте и перезапустите бэкенд:

```bash
# Отредактируйте .env
nano /var/www/studnet/backend_python/.env

# Добавьте/обновите:
CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://rica-student-trusted-puzzle.trycloudflare.com

# Перезапустите бэкенд
systemctl restart studnet-backend.service
```

### 5. Проверьте Cloudflare Tunnel:

Убедитесь, что туннель проксирует на правильный порт (8000):

```bash
# Проверьте как запущен туннель
ps aux | grep cloudflared

# Должен проксировать на http://localhost:8000
```

## 🔧 Быстрое исправление:

### Шаг 1: Проверьте .env на фронтенде

```bash
# На вашем компьютере
cat .env
# Должно быть: REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com
```

### Шаг 2: Перезапустите фронтенд

```bash
# Остановите (Ctrl+C) и запустите снова
npm start
```

### Шаг 3: Проверьте CORS на бэкенде

```bash
# На сервере
ssh root@155.212.170.255
cat /var/www/studnet/backend_python/.env | grep CORS_ORIGINS
```

Если нет домена туннеля - добавьте и перезапустите бэкенд.

### Шаг 4: Проверьте доступность

В браузере откройте консоль (F12) и проверьте:
- Какой URL используется
- Какие ошибки

## 📋 Диагностика в браузере:

1. Откройте DevTools (F12)
2. Вкладка Console - проверьте логи
3. Вкладка Network - проверьте запросы:
   - Какой URL используется?
   - Какой статус ответа?
   - Есть ли CORS ошибки?

## 🎯 Наиболее вероятные причины:

1. **Фронтенд не перезапущен** после изменения `.env` - нужно перезапустить `npm start`
2. **CORS не настроен** на бэкенде для домена туннеля
3. **Cloudflare Tunnel не проксирует** правильно на порт 8000

