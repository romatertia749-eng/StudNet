# Исправление проблемы с портом бэкенда

## 🔍 Обнаруженная проблема:

1. **Бэкенд слушает на порту 8000**, а не 8080
2. **Привязан к 127.0.0.1:8000** (только localhost)
3. **Cloudflare Tunnel** должен проксировать на правильный порт

## ✅ Проверка на правильном порту:

```bash
# Проверка на порту 8000
curl http://localhost:8000/health
curl http://127.0.0.1:8000/health
```

## 🔧 Исправления:

### Вариант 1: Обновить Cloudflare Tunnel (если он проксирует на 8080)

Проверьте конфигурацию туннеля:

```bash
# Найдите конфигурацию туннеля
find /root -name "*.yaml" -o -name "config.json" | grep -i cloudflare

# Или проверьте как запущен туннель
ps aux | grep cloudflared
```

Туннель должен проксировать на `http://localhost:8000`, а не 8080.

### Вариант 2: Изменить порт бэкенда на 8080

Если хотите использовать порт 8080:

```bash
# Найдите конфигурацию gunicorn
find /var/www/studnet -name "*.service" -o -name "gunicorn.conf" -o -name ".env" | head -5

# Проверьте systemd service
systemctl cat studnet-backend.service
```

Измените `--bind 127.0.0.1:8000` на `--bind 0.0.0.0:8080` в конфигурации.

### Вариант 3: Обновить фронтенд (использовать порт 8000)

Если оставляете порт 8000, обновите `.env` на фронтенде:

```env
REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com
```

Но убедитесь, что Cloudflare Tunnel проксирует на порт 8000.

## 🚀 Быстрая проверка:

```bash
# 1. Проверка health на правильном порту
curl http://localhost:8000/health

# 2. Проверка через Cloudflare Tunnel
curl https://rica-student-trusted-puzzle.trycloudflare.com/health

# 3. Проверка логов бэкенда
tail -20 /var/log/studnet/backend_error.log
tail -20 /var/log/studnet/backend_access.log

# 4. Проверка systemd service
systemctl status studnet-backend.service
```

## 📝 Рекомендация:

**Лучше всего**: Обновить Cloudflare Tunnel, чтобы он проксировал на `http://localhost:8000` вместо 8080.

Или изменить бэкенд на порт 8080 и привязку к `0.0.0.0` для доступности через туннель.

