# Быстрая настройка подключения к бэкенду

> **Для Cloudflare Tunnel:** См. `CLOUDFLARE_TUNNEL_SETUP.md` и `CLOUDFLARE_TUNNEL_BACKEND_SETUP.md`

## ✅ Что было сделано автоматически

1. **Автоматическая проверка бэкенда** - перед отправкой формы профиль проверяет доступность сервера
2. **Компонент диагностики** - при ошибках показывается подробная информация о проблеме
3. **Улучшенные сообщения об ошибках** - теперь показывают URL и рекомендации по исправлению
4. **Скрипт проверки** - можно проверить бэкенд вручную через командную строку

## 🚀 Быстрый старт

### 1. Проверьте, запущен ли бэкенд

```bash
# Перейдите в папку бэкенда
cd backend_python

# Активируйте виртуальное окружение (если есть)
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Запустите бэкенд
uvicorn app.main:app --reload --port 8080
```

### 2. Проверьте подключение

```bash
# В корне проекта (где package.json)
npm run check-backend
```

Или откройте в браузере: `http://localhost:8080/health`

Должен вернуться: `{"status":"ok"}`

### 3. Настройте переменную окружения

Создайте файл `.env` в корне проекта (рядом с `package.json`):

```env
REACT_APP_API_BASE_URL=http://localhost:8080
```

### 4. Перезапустите фронтенд

```bash
npm start
```

## 🔍 Диагностика проблем

### Проблема: "Failed to Fetch" при создании профиля

**Решение:**
1. Убедитесь, что бэкенд запущен (см. шаг 1)
2. Проверьте `.env` файл (см. шаг 3)
3. Запустите `npm run check-backend` для диагностики

### Проблема: "Ошибка сети" при переходе в анкеты

**Решение:**
1. Проверьте, что бэкенд отвечает на `/health`
2. Проверьте CORS настройки в бэкенде
3. Убедитесь, что `REACT_APP_API_BASE_URL` указан правильно

## 📝 Где находится бэкенд?

Бэкенд находится в папке `backend_python/`:

```
max-networking-app/
├── backend_python/          ← Бэкенд здесь
│   ├── app/
│   │   ├── main.py         ← Главный файл FastAPI
│   │   └── ...
│   └── requirements.txt
├── src/                     ← Фронтенд здесь
└── package.json
```

## 🎯 Основные эндпоинты

- **Health check**: `http://localhost:8080/health`
- **Профили**: `http://localhost:8080/api/profiles/`
- **Мэтчи**: `http://localhost:8080/api/matches`

## 💡 Автоматическая диагностика

При ошибке подключения автоматически показывается компонент `BackendStatus` с:
- Статусом подключения
- URL сервера
- Детальной информацией об ошибке
- Рекомендациями по исправлению

## 📚 Подробная документация

См. `BACKEND_CHECK.md` для детальной информации о диагностике и решении проблем.

