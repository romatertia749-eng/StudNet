# Настройка бэкенда для Cloudflare Tunnel

## ✅ Фронтенд уже настроен

Файл `.env` создан с правильным URL:
```env
REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com
```

## 🔧 Что нужно сделать на бэкенде

### 1. Убедитесь, что бэкенд запущен

На вашем сервере запустите бэкенд:

```bash
cd backend_python
uvicorn app.main:app --host 0.0.0.0 --port 8080
```

### 2. Настройте переменные окружения бэкенда

Создайте или обновите файл `.env` в папке `backend_python/`:

```env
# Database
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret
JWT_SECRET=your-secret-key-change-in-production-min-32-chars

# CORS - ВАЖНО: добавьте домен туннеля
CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://rica-student-trusted-puzzle.trycloudflare.com

# Production флаг
PRODUCTION=true

# Остальные настройки (ImageKit, Telegram и т.д.)
IMAGEKIT_PUBLIC_KEY=your_public_key_here
IMAGEKIT_PRIVATE_KEY=your_private_key_here
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_imagekit_id
```

### 3. Убедитесь, что Cloudflare Tunnel настроен правильно

Туннель должен проксировать запросы на ваш локальный бэкенд:

```bash
# Пример команды для cloudflared
cloudflared tunnel --url http://localhost:8080
```

Или если используете конфигурационный файл:

```yaml
tunnel: your-tunnel-id
credentials-file: /path/to/credentials.json

ingress:
  - hostname: rica-student-trusted-puzzle.trycloudflare.com
    service: http://localhost:8080
  - service: http_status:404
```

### 4. Проверьте доступность

После настройки проверьте:

```bash
# Из терминала
curl https://rica-student-trusted-puzzle.trycloudflare.com/health

# Или откройте в браузере
https://rica-student-trusted-puzzle.trycloudflare.com/health
```

Должен вернуться: `{"status":"ok"}`

## 🔍 Диагностика проблем

### Проблема: Таймаут при подключении

**Возможные причины:**
1. Бэкенд не запущен на сервере
2. Туннель не активен или настроен неправильно
3. Порт 8080 не открыт или занят

**Решение:**
1. Проверьте, что бэкенд запущен: `ps aux | grep uvicorn`
2. Проверьте, что туннель активен: `cloudflared tunnel list`
3. Проверьте логи туннеля на ошибки

### Проблема: CORS ошибка

**Причина:** Бэкенд не разрешает запросы с фронтенда

**Решение:**
1. Убедитесь, что в `.env` бэкенда указан правильный `CORS_ORIGINS`
2. Перезапустите бэкенд после изменения `.env`
3. Проверьте логи бэкенда на CORS ошибки

### Проблема: 404 Not Found

**Причина:** Туннель не проксирует запросы на правильный порт

**Решение:**
1. Проверьте конфигурацию туннеля
2. Убедитесь, что бэкенд слушает на `0.0.0.0:8080` (не `127.0.0.1`)
3. Проверьте, что туннель указывает на правильный порт

## 📝 Быстрая проверка

1. **Проверьте бэкенд локально:**
   ```bash
   curl http://localhost:8080/health
   ```

2. **Проверьте через туннель:**
   ```bash
   curl https://rica-student-trusted-puzzle.trycloudflare.com/health
   ```

3. **Проверьте из фронтенда:**
   ```bash
   npm run check-backend https://rica-student-trusted-puzzle.trycloudflare.com
   ```

## ⚠️ Важно

- **HTTPS обязателен** - Cloudflare Tunnel использует HTTPS
- **CORS должен разрешать** домен туннеля и Telegram домены
- **Бэкенд должен слушать на `0.0.0.0`**, а не `127.0.0.1`
- **Туннель должен быть активен** пока работает бэкенд

## 🔄 После настройки

1. Перезапустите бэкенд с новыми настройками
2. Убедитесь, что туннель активен
3. Проверьте health endpoint
4. Перезапустите фронтенд (если нужно)
5. Попробуйте создать профиль в приложении

