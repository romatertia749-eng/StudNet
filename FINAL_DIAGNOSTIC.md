# Финальная диагностика проблемы

## ✅ Проверка на сервере:

```bash
# 1. Проверьте, работает ли бэкенд
curl http://127.0.0.1:8000/health

# 2. Проверьте статус service
systemctl status studnet-backend.service --no-pager | head -10

# 3. Проверьте доступность через Cloudflare Tunnel
curl https://rica-student-trusted-puzzle.trycloudflare.com/health

# 4. Проверьте доступность через nginx
curl http://localhost/api/health

# 5. Проверьте логи бэкенда (последние ошибки)
tail -20 /var/log/studnet/backend_error.log
```

## 🔍 Проверка в приложении:

### 1. Откройте консоль браузера:

- В браузере нажмите F12 (или правый клик → Inspect)
- Перейдите на вкладку **Console**
- Попробуйте создать профиль
- Посмотрите, какие ошибки появляются

### 2. Проверьте Network вкладку:

- В DevTools перейдите на вкладку **Network**
- Попробуйте создать профиль
- Найдите запрос к `/api/profiles/`
- Посмотрите:
  - Какой URL используется?
  - Какой статус ответа?
  - Есть ли CORS ошибки?

### 3. Проверьте переменные окружения:

В консоли браузера выполните:
```javascript
console.log('API URL:', process.env.REACT_APP_API_BASE_URL);
```

Или проверьте в коде - откройте `src/config/api.js` и посмотрите, что выводится в консоль при загрузке.

## 🚀 Полная проверка (выполните на сервере):

```bash
echo "=== БЭКЕНД ==="
curl -s http://127.0.0.1:8000/health
echo ""
systemctl is-active studnet-backend.service && echo "Service: OK" || echo "Service: ERROR"
echo ""

echo "=== CLOUDFLARE TUNNEL ==="
curl -s -m 5 https://rica-student-trusted-puzzle.trycloudflare.com/health || echo "НЕДОСТУПЕН"
echo ""

echo "=== NGINX ==="
curl -s -m 5 http://localhost/api/health || echo "НЕДОСТУПЕН"
echo ""

echo "=== ЛОГИ (последние ошибки) ==="
tail -10 /var/log/studnet/backend_error.log | grep -i error | tail -5
```

## 📋 Что проверить в приложении:

1. **Перезапустите фронтенд** после изменения `.env`:
   ```bash
   # Остановите (Ctrl+C) и запустите снова
   npm start
   ```

2. **Проверьте .env файл**:
   ```bash
   cat .env
   # Должно быть: REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com
   ```

3. **Очистите кеш браузера** или откройте в режиме инкогнито

4. **Проверьте консоль браузера** - там будут детали ошибки

