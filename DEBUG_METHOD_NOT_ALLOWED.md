# Диагностика ошибки "Method not Allowed"

## Что уже исправлено

1. ✅ Добавлена поддержка обоих вариантов URL (`/api/profiles` и `/api/profiles/`)
2. ✅ Роуты настроены правильно
3. ✅ CORS настроен

## 🔍 Диагностика

### 1. Проверь URL во фронтенде

Открой DevTools (F12) → Console и найди логи:
- `API_BASE_URL: ...` - должен быть `https://married-perl-dk-it1-106c0464.koyeb.app`
- `API_ENDPOINTS.PROFILES: ...` - должен быть `https://married-perl-dk-it1-106c0464.koyeb.app/api/profiles`

**Если видишь `http://localhost:8080`** → проблема в переменной окружения `REACT_APP_API_BASE_URL` на Vercel.

### 2. Проверь Network tab

1. Открой DevTools (F12) → Network
2. Попробуй создать профиль
3. Найди запрос к `/api/profiles`
4. Проверь:
   - **Request URL**: должен быть `https://married-perl-dk-it1-106c0464.koyeb.app/api/profiles`
   - **Request Method**: должен быть `POST`
   - **Status Code**: что показывает? (405, 404, 500?)
   - **Request Headers**: проверь `Content-Type` (должен быть `multipart/form-data` с boundary)
   - **Response**: что в ответе?

### 3. Проверь через Swagger UI

Открой в браузере:
```
https://married-perl-dk-it1-106c0464.koyeb.app/docs
```

Найди `POST /api/profiles/` и попробуй отправить тестовый запрос:
- Заполни все обязательные поля
- Попробуй отправить

**Если через Swagger работает** → проблема во фронтенде (URL или CORS)
**Если через Swagger не работает** → проблема в бэкенде

### 4. Проверь логи в Koyeb

1. Зайди в Koyeb Dashboard → твой app → Logs
2. Попробуй создать профиль
3. Ищи в логах:
   - Запросы к `/api/profiles`
   - Ошибки
   - Статус коды

### 5. Проверь CORS

В Network tab проверь:
- Есть ли запрос `OPTIONS /api/profiles` перед `POST`?
- Какой статус у `OPTIONS` запроса? (должен быть 200)
- Если `OPTIONS` возвращает ошибку → проблема в CORS

## 🚨 Возможные причины

### Причина 1: Неправильный URL во фронтенде
**Решение:** Проверь переменную `REACT_APP_API_BASE_URL` в Vercel

### Причина 2: CORS блокирует запрос
**Решение:** 
- Проверь `CORS_ORIGINS` в Koyeb
- Добавь `https://stud-net.vercel.app` в `CORS_ORIGINS`

### Причина 3: Catch-all роут перехватывает запрос
**Решение:** Уже исправлено - catch-all только для GET

### Причина 4: Проблема с порядком роутов
**Решение:** Уже исправлено - API роуты регистрируются первыми

## 📝 Что сделать сейчас

1. **Закоммить изменения:**
   ```bash
   git add backend_python/app/routers/profiles.py backend_python/app/routers/auth.py src/pages/ProfileForm.jsx
   git commit -m "Add support for both /api/profiles and /api/profiles/ URLs"
   git push
   ```

2. **Проверить переменные в Vercel:**
   - `REACT_APP_API_BASE_URL=https://married-perl-dk-it1-106c0464.koyeb.app`
   - Пересобрать проект

3. **Проверить CORS в Koyeb:**
   - `CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://stud-net.vercel.app`

4. **Проверить через Swagger UI:**
   - Открой `/docs` и попробуй создать профиль

5. **Проверить логи:**
   - В браузере (Console и Network)
   - В Koyeb (Logs)

## 🔗 Полезные ссылки

- Swagger UI: https://married-perl-dk-it1-106c0464.koyeb.app/docs
- Health check: https://married-perl-dk-it1-106c0464.koyeb.app/health
- Koyeb Dashboard: https://www.koyeb.com/apps

