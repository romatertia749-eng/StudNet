# 🚀 КОМАНДЫ ДЛЯ ДЕПЛОЯ НА СЕРВЕР

## 📋 ЧТО НУЖНО СДЕЛАТЬ:

### 1. **НА ЛОКАЛЬНОЙ МАШИНЕ (Windows):**

```bash
# Проверить, что вы на правильном коммите
git log --oneline -1

# Должно показать: 43f15ce фотография отображается на всю ширину

# Если нужно, закоммитить изменения (если есть)
git add .
git commit -m "Откат до стабильной версии"
```

### 2. **ОТПРАВИТЬ ИЗМЕНЕНИЯ В РЕПОЗИТОРИЙ:**

```bash
# ВАЖНО: Это перезапишет удаленную ветку!
git push origin main --force
```

⚠️ **ВНИМАНИЕ:** `--force` удалит все коммиты после "43f15ce" на удаленном репозитории!

### 3. **НА СЕРВЕРЕ (Linux):**

```bash
# Подключиться к серверу
ssh root@155.212.170.255

# Перейти в директорию проекта
cd /var/www/studnet

# Откатить проект до нужного коммита
git fetch origin
git reset --hard origin/main

# Установить зависимости (если нужно)
npm install

# Собрать проект
npm run build

# Установить права на файлы
chown -R www-data:www-data build
chmod -R 755 build

# Перезагрузить Nginx
systemctl reload nginx
```

### 4. **ОДНОЙ КОМАНДОЙ (если есть доступ по SSH):**

```bash
ssh root@155.212.170.255 "cd /var/www/studnet && git fetch origin && git reset --hard origin/main && npm install && npm run build && chown -R www-data:www-data build && chmod -R 755 build && systemctl reload nginx"
```

## ✅ ПРОВЕРКА:

После выполнения команд проверьте:

1. **Работает ли сайт:**
   ```bash
   curl -I http://155.212.170.255/
   ```

2. **Работает ли API:**
   ```bash
   curl http://155.212.170.255/api/health
   ```

3. **Логи Nginx (если есть ошибки):**
   ```bash
   tail -f /var/log/nginx/error.log
   ```

## 🔧 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

1. **Проверьте логи бэкенда:**
   ```bash
   journalctl -u studnet-backend -n 50
   ```

2. **Проверьте конфигурацию Nginx:**
   ```bash
   nginx -t
   ```

3. **Перезапустите бэкенд (если нужно):**
   ```bash
   systemctl restart studnet-backend
   ```

