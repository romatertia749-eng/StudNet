# Быстрое развертывание фронтенда

## ❌ Ошибка: "Could not read package.json"

Вы находитесь не в директории фронтенда. Фронтенд должен быть развернут отдельно от бэкенда.

## ✅ Решение: Разверните фронтенд на Vercel (5 минут)

### Шаг 1: На локальном компьютере

```bash
# Убедитесь, что .env настроен
cat .env
# Должно быть: REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com

# Установите Vercel CLI (один раз)
npm install -g vercel

# Войдите в Vercel
vercel login
```

### Шаг 2: Разверните

```bash
# Из корня проекта (где package.json)
vercel
```

Следуйте инструкциям:
- Set up and deploy? **Y**
- Which scope? Выберите ваш аккаунт
- Link to existing project? **N**
- What's your project's name? (нажмите Enter для дефолтного)
- In which directory is your code located? **./** (просто точка)
- Want to override settings? **N**

### Шаг 3: Установите переменную окружения

1. Откройте https://vercel.com/dashboard
2. Выберите ваш проект
3. Settings → Environment Variables
4. Добавьте:
   - **Key:** `REACT_APP_API_BASE_URL`
   - **Value:** `https://rica-student-trusted-puzzle.trycloudflare.com`
   - **Environment:** Production, Preview, Development (все три)

5. Переразверните:
   ```bash
   vercel --prod
   ```

## 🎯 Готово!

Теперь фронтенд доступен по URL, который даст Vercel (например: `https://your-app.vercel.app`)

## 🔄 Альтернатива: Если хотите на том же сервере

Если фронтенд уже скопирован на сервер, найдите его:

```bash
# Найдите package.json
find / -name "package.json" -type f 2>/dev/null | grep -v node_modules

# Перейдите в найденную директорию
cd /путь/к/фронтенду

# Но помните: для продакшена используйте build, а не npm start!
npm run build
# Затем настройте nginx для раздачи build/
```

## 📚 Подробнее

См. `DEPLOY_FRONTEND.md` для детальной инструкции.

