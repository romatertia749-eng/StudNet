# 🚀 БЫСТРЫЙ ДЕПЛОЙ - ВАЖНЫЙ ФАЙЛ!

> ⚠️ **НЕ УДАЛЯТЬ!** Этот файл содержит инструкции для деплоя на Netlify + Koyeb + Neon.

## 🚀 Автоматический деплой (Netlify + Koyeb)

После настройки деплой происходит автоматически при push в GitHub!

### 📋 Первоначальная настройка (один раз):

1. **База данных (Neon)**:
   - Создайте проект на [neon.tech](https://neon.tech)
   - Примените схему из `database/schema.sql` через SQL Editor
   - Скопируйте connection string

2. **Бэкенд (Koyeb)**:
   - Подключите GitHub репозиторий
   - Root Path: `backend_python`
   - Настройте Environment Variables (см. `NETLIFY_DEPLOY.md`)
   - Скопируйте Koyeb URL

3. **Фронтенд (Netlify)**:
   - Подключите GitHub репозиторий
   - Build settings автоматически из `netlify.toml`
   - Environment Variable: `REACT_APP_API_BASE_URL` = ваш Koyeb URL
   - Скопируйте Netlify URL

4. **Обновите переменные**:
   - В Koyeb: `FRONTEND_URL` = ваш Netlify URL
   - В Netlify: `REACT_APP_API_BASE_URL` = ваш Koyeb URL

## ⚡ Обычный деплой (после настройки):

```powershell
# 1. Коммит и push
git add .
git commit -m "Описание изменений"
git push origin main
```

**Всё!** Netlify и Koyeb автоматически задеплоят изменения.

## 📝 Чек-лист для каждого обновления:

- [ ] Изменения внесены и протестированы локально
- [ ] Код закоммичен в Git
- [ ] Код отправлен в GitHub (`git push`)
- [ ] Проверено, что Netlify задеплоил фронтенд
- [ ] Проверено, что Koyeb задеплоил бэкенд
- [ ] Проверено в приложении Telegram

## ⚠️ Важно:

1. **Фронтенд (Vercel)** - автоматический деплой из `main` ветки
2. **Бэкенд (Koyeb)** - автоматический деплой из `main` ветки
3. **База данных (Neon)** - не требует деплоя, только миграции через SQL Editor
4. **Переменные окружения** - настраиваются в панелях Vercel и Koyeb

## 🔧 Если что-то не работает:

1. Проверьте логи в Vercel Dashboard → Deployments
2. Проверьте логи в Koyeb Dashboard → Logs
3. Проверьте переменные окружения в обеих панелях
4. Убедитесь, что `DATABASE_URL` правильный (Neon connection string)

Подробнее: `QUICK_MIGRATION.md`


