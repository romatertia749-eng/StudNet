# 🚀 БЫСТРЫЙ ДЕПЛОЙ - ВАЖНЫЙ ФАЙЛ!

> ⚠️ **НЕ УДАЛЯТЬ!** Этот файл содержит команды для быстрого деплоя.

## 🚀 Одна команда для деплоя фронтенда:

```powershell
# На вашем компьютере
cd C:\Users\Lenovo\max-networking-app
npm run build && scp -r build/* root@155.212.170.255:/var/www/studnet/public/
```

## 📋 Полный процесс (пошагово):

### 1. Сохраните изменения в Git:

```powershell
git add .
git commit -m "Описание изменений"
git push origin main
```

### 2. Деплой фронтенда:

```powershell
npm run build
scp -r build/* root@155.212.170.255:/var/www/studnet/public/
```

### 3. Деплой бэкенда (если меняли):

```bash
ssh root@155.212.170.255
cd /var/www/studnet/backend_python
git pull origin main
systemctl restart studnet-backend.service
```

## 🔄 Или используйте скрипт deploy.sh:

```powershell
# На вашем компьютере (если у вас есть bash)
bash deploy.sh frontend    # Только фронтенд
bash deploy.sh backend     # Только бэкенд
bash deploy.sh all         # Всё
```

## ⚡ Самый быстрый способ:

```powershell
# 1. Коммит и push
git add . && git commit -m "Update" && git push origin main

# 2. Деплой фронтенда
npm run build && scp -r build/* root@155.212.170.255:/var/www/studnet/public/
```

## 📝 Чек-лист для каждого обновления:

- [ ] Изменения внесены и протестированы локально
- [ ] Код закоммичен в Git
- [ ] Код отправлен в GitHub (`git push`)
- [ ] Фронтенд собран (`npm run build`)
- [ ] Фронтенд загружен на сервер (`scp build/*`)
- [ ] Бэкенд обновлен (если меняли) (`git pull` на сервере)
- [ ] Бэкенд перезапущен (если меняли) (`systemctl restart`)
- [ ] Проверено в приложении Telegram

## ⚠️ Важно:

1. **Фронтенд** - всегда нужно собирать (`npm run build`) и загружать на сервер
2. **Бэкенд** - можно обновлять через `git pull` на сервере
3. **GitHub** - это хранилище кода, не автоматический деплой
4. **Сервер** - это продакшн, туда загружаются собранные файлы

Это всё, что нужно для обновления!

