# Настройка для Cloudflare Tunnel

Ваш бэкенд доступен через Cloudflare Tunnel по адресу:
**https://rica-student-trusted-puzzle.trycloudflare.com/**

## ✅ Что было настроено

1. **Фронтенд (.env)** - создан файл с правильным URL бэкенда
2. **CORS настройки** - нужно обновить на бэкенде

## 🔧 Настройка бэкенда

### 1. Обновите переменные окружения бэкенда

В файле `.env` бэкенда (или в переменных окружения на сервере) добавьте:

```env
# URL фронтенда (если фронтенд на отдельном домене)
FRONTEND_URL=https://your-frontend-url.com

# CORS origins - добавьте домен фронтенда и Telegram домены
CORS_ORIGINS=https://web.telegram.org,https://telegram.org,https://rica-student-trusted-puzzle.trycloudflare.com

# Production флаг
PRODUCTION=true
```

### 2. Перезапустите бэкенд

После изменения переменных окружения перезапустите бэкенд:

```bash
# Если используете systemd
sudo systemctl restart your-backend-service

# Или если запускаете вручную
# Остановите текущий процесс и запустите снова
uvicorn app.main:app --host 0.0.0.0 --port 8080
```

## 🔍 Проверка подключения

### Проверка health endpoint

Откройте в браузере:
```
https://rica-student-trusted-puzzle.trycloudflare.com/health
```

Должен вернуться: `{"status":"ok"}`

### Проверка через скрипт

```bash
npm run check-backend https://rica-student-trusted-puzzle.trycloudflare.com
```

## ⚠️ Важные замечания

### Cloudflare Tunnel URL может меняться

URL Cloudflare Tunnel (`*.trycloudflare.com`) может меняться при перезапуске туннеля. 

**Решение:**
1. Используйте постоянный домен Cloudflare (если доступен)
2. Или обновляйте `.env` файл при каждом изменении URL
3. Или используйте переменную окружения на сервере фронтенда

### HTTPS обязателен

Cloudflare Tunnel использует HTTPS, поэтому:
- ✅ Все запросы должны идти через `https://`
- ✅ CORS должен разрешать HTTPS домены
- ✅ Сертификаты валидны автоматически

### CORS настройки

Убедитесь, что бэкенд разрешает запросы с:
- Домена фронтенда (если на отдельном домене)
- `https://web.telegram.org` (для Telegram Web App)
- `https://telegram.org` (для Telegram Web App)

## 📝 Текущая конфигурация

**Фронтенд (.env):**
```env
REACT_APP_API_BASE_URL=https://rica-student-trusted-puzzle.trycloudflare.com
```

**Бэкенд должен разрешать CORS для:**
- `https://web.telegram.org`
- `https://telegram.org`
- Домен вашего фронтенда (если на отдельном домене)

## 🚀 Быстрая проверка

1. Проверьте health endpoint:
   ```bash
   curl https://rica-student-trusted-puzzle.trycloudflare.com/health
   ```

2. Проверьте из фронтенда:
   ```bash
   npm run check-backend https://rica-student-trusted-puzzle.trycloudflare.com
   ```

3. Откройте приложение и попробуйте создать профиль

## 🔄 Если URL туннеля изменился

1. Обновите `.env` файл:
   ```env
   REACT_APP_API_BASE_URL=https://новый-url.trycloudflare.com
   ```

2. Перезапустите фронтенд:
   ```bash
   npm start
   ```

3. Обновите CORS на бэкенде (если нужно)

